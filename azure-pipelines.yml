# Docker image
# Build a Docker image to deploy, run, or push to a container registry.
# Add steps that use Docker Compose, tag images, push to a registry, run an image, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
- group: azuresecrets
  imageName: 'iot-device-net:$(build.buildId)'
  provisionImage: 'iot-device-net-provision:$(build.buildId)'

steps:
- script: docker build -f Dockerfile -t $(imageName) .
  displayName: 'Docker Build Image'
- script: docker build -f Dockerfile-provision -t $(provisionImage) .
  displayName: 'Docker Build Provision'
- script: |
    docker login -u $(clientId) -p $(clientSecret) $(containerRegistry)
    docker tag $(imageName) $(containerRegistry)/iot-device-net:latest
    docker tag $(provisionImage) $(containerRegistry)/iot-device-net:provision
    docker push $(containerRegistry)/$(imageName)
  displayName: "Docker Push Images"
